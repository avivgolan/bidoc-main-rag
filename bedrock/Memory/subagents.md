---
note_type: durable-memory-branch
project: bidoc agent
branch: subagents
last_updated: 2026-08-05
tags:
  - subagents
  - alert
  - meeting-evidence
  - delay-claim
  - data-query
---

# Subagents

## Current State

- The Tool Testing diagnostics page includes a dedicated internal-subagents group covering 14 operational subagents. Each check is an individual read-only readiness probe, reports active/error/inactive explicitly, and can be rerun without database writes.
- The Alert subagent lives in `src/subagents/alert.js`.
- Alert subagent settings are stored under `settings.subagents.alert`; the UI for them lives in the Subagents tab in `public/app.js`.
- The Alert subagent searches the configured Supabase embeddings table, defaulting to `alerts_embeddings_gf`, through an RPC named `match_<table>`.
- The test endpoint `POST /api/subagents/alert` is handled in `src/server.js`.
- Alert requests can include `dateFilter`, `dateFrom`, and `dateTo`.
- When `dateFrom`/`dateTo` are present, Alert filters returned rows by `date`, `metadata.date`, `created_at`, or `metadata.created_at`.
- The workflow UI has a first-class `alert_agent` node for Alert runs.
- The database-agent specification is stored at `prompts agents - dor/bidoc-principal-database-architect.md`; it defines PostgreSQL/Supabase ownership, database safety rules, and the Hebrew reporting contract.
- The Meeting Evidence Agent specification is stored at `docs/meeting-evidence-agent-spec.md`; its Phase 4D note supersedes the original design where needed. Live semantic retrieval targets only `public.meetings_documents`; the temporary structural-RPC fallback performs no adjacency expansion, and exact mixed evidence requires same-meeting identity attestation.
- The contractor delay-claim agent roadmap is stored at `docs/contractor-delay-claim-agents-roadmap.md`; it splits the proposed claim-building agent system into four implementation stages aligned with the existing Node/SPA, Supabase, Timeline, Project Graph, Workflow, and Meeting Evidence Agent infrastructure.
- The Delay Claim subagent lives in `src/subagents/delayClaim.js`; Stage 2 maps sources, builds chronology, detects/merges delay event candidates, collects evidence, detects gaps/contradictions, and emits workflow-log nodes without legal, cost, entitlement, or schedule-critical-path conclusions.
- Delay Claim Stage 3 deep analysis also lives in `src/subagents/delayClaim.js`; it analyzes existing events for causality, notices, possible responsibility, concurrency, mitigation, attack risk, readiness, and quality review while persisting candidate findings only.
- Delay Claim Stage 4 package preparation also lives in `src/subagents/delayClaim.js`; it creates schedule review links, possible cost items, dashboard metrics, and Markdown/JSON exports without final entitlement or critical-path conclusions.
- The Data Query Agent implementation prompt is stored at `docs/data-query-agent-codex-prompt.md`; it specifies a read-only, allowlisted Query Plan subagent with multi-query support, safe Supabase execution, Main Agent integration, Workflow visibility, Settings UI, and tests.
- The Data Query Agent lives in `src/subagents/dataQuery.js`; it plans and validates canonical typed Query Plans, rejects raw SQL and joins, uses the fixed exact RPC for `data_index`, and uses reviewed credential-gated PostgREST `GET`/`HEAD` transports for `financial_transactions`, `safety_reports`, `alerts`, and `meetings`.
- Data Query typed field capabilities and sensitivity rules live in `src/subagents/dataQueryMetadata.js`. Content fields are excluded from quantitative planning, and unregistered exact operations return `not_computable`.
- Data Query Agent settings are exposed under `settings.subagents.dataQuery` with enabled, LLM planner, planner model/timeout, plan/row limits, timeouts, and Content-only allowed tables. Dead raw-SQL, join, and approval flags were removed in Phase 0.
- The Data Query Agent LLM planner uses OpenRouter JSON-object output against a compact schema manifest and falls back to the deterministic heuristic planner if the model is unavailable or returns an invalid/unsafe plan.
- The direct Data Query Agent endpoint is `POST /api/subagents/data-query`; it fails closed unless `BIDOC_API_SECRET` is configured and supplied as `X-Bidoc-Api-Secret`.
- The Subagents tab in `public/app.js` includes a Data Query Agent card for Content table settings. Direct browser execution and schema scanning were retired; testing uses the Main Agent or the authenticated HTTP endpoint.
- The workflow UI and chat workflow logs include a `data_query` database node when the Main Agent invokes the Data Query Agent.
- The direct Data Query Agent test (`POST /api/subagents/data-query`) now creates its own run: it calls `createRun`, emits `data_query` run events per phase, builds a workflow log via `buildDataQueryWorkflowLog`, and records run history with `kind: "data_query"` so the run appears in the Workflow tab strip just like chat/insights/link-agent runs.
- `buildDataQueryWorkflowLog(result, { question, context })` in `src/subagents/dataQuery.js` builds the Data Query workflow graph. Execution-node details retain the accepted plan, row count/status, and field names but never raw source-row previews.
- Data Query tables are provisioned deliberately in settings and in the database migration. The browser no longer performs privileged OpenAPI schema discovery.
- The `content` schema alias is a connection selector, not a Postgres schema. It points only to Kapaim Content; the actual approved table is in Postgres schema `public`.
- When the user picks tables, they are saved to `settings.subagents.dataQuery.tables` ([{ connection, schema, table, columns }]). At runtime, `dataQuerySettings()` derives the selected manifest and merges only credential-active reviewed managed-read built-ins; a reviewed built-in replaces a same-name selected definition without writing settings or changing the saved selection. If no selection is saved, the reviewed built-in manifest is used. The browser performs no privileged schema discovery.
- The Subagents Data Query card shows the saved Content table selection without a privileged scan or direct-run control.
- Phase 0 retired the model-generated SQL pipeline. Its helpers are private compatibility code only; the step/pipeline endpoints and UI are removed, and the typed Query Plan path is the sole supported runtime.
- The Kapaim `public.exec_read_sql(text, integer)` RPC was dropped in the live Phase 0/1 migration. The tracked `supabase/data-query-exec-read-sql.sql` now decommissions the RPC and provisions the least-privilege `bidoc_data_query` role instead.
- The canonical Query Plan runtime never emits or executes SQL and cannot join across connections.
- The `hashtags` column exists in `data_index_embeddings_gf_dor_agent` (text) and many `_gf` tables (emails_gf is an ARRAY type), but NOT in `alerts`/`alerts_gf`. So a "count delays by hashtag" question must target `data_index_...`, not the alerts tables — selecting the right table in the picker matters.
- The Data Query Agent remains restricted to the logical `content` schema and fixed GET/HEAD table contracts. When Settings explicitly sets `contentSource.useAppSupabase`, that logical connection resolves to MAIN and the approved fixed transport may use the server-side MAIN service key; arbitrary tables, fields, methods, bodies, and SQL remain blocked.
- The current localhost Settings map Content to MAIN (`pmdnmzuqbcnzgkuhpfnx`). The former separate Kapaim connection (`smxibuaowzuxkznuouwj`) is historical and its saved key was invalid during the 2026-08-05 audit.

- The current-state audit is `docs/data-query-agent-current-state-audit.md`; its Phase 0/1 status section records the implemented security baseline. Operator and deployment instructions are in `docs/data-query-agent-phase0-phase1-operations.md`.
- The Phase 2 exactness contract and live/gold verification evidence are in `docs/data-query-agent-phase2-correctness.md`.
- Data Query Phase 3 uses the versioned `data-query.v2` caller/response contract. It normalizes caller source/run identity, only narrows caller budgets, enforces project/date scope, routes semantic/citation questions without database execution, deduplicates identical plans within one run, and returns `machineResult.metricsByRequestId` for machine consumers.
- Main Agent passes its run ID and caller node to Data Query and must consume numeric facts from `machineResult`, not the display-oriented `answer`.
- Phase 3 contract and verification details are in `docs/data-query-agent-phase3-contract.md`.
- Data Query Phase 3.1 uses a dedicated Supabase Auth service account with `app_metadata.data_query_role=bidoc_data_query`. The runtime validates, caches, and refreshes short-lived tokens; the static custom-role JWT is compatibility-only.
- The live Phase 3.1 wrapper is claim-gated `SECURITY DEFINER` with an empty search path. `bidoc_data_query` remains `NOLOGIN` and has no direct table/sequence privileges; API roles cannot execute the fixed-table implementation function.
- Phase 3.1 intended a missing optional table selection to fall back to the exact `data_index` contract rather than the hybrid-search embedding table. Phase 4A.0 found that the current fallback still constructs two additional non-exact manifest entries; see the correction below.
- Direct managed-service-account token provisioning still rejects `contentSource.usesAppSupabase=true`; the explicit MAIN mapping instead uses only the server-side service key inside the fixed read-only Data Query transport.
- Phase 3.1 implementation and provisioning instructions are in `docs/data-query-agent-phase3-1-managed-auth.md`.
- Data Query Phase 4A.0 is a read-only structured-capability map. Exact analytics remain limited to `data_index`; `latest`/`earliest`/`last N` need a typed lookup operation and machine-readable record contract before any business table is promoted.
- Phase 4A.0 found that the real no-selection fallback built `data_index`, configured alerts, and `meetings_documents`, while the old test manually forced `allowedTables: ["data_index"]`. Phase 4A.1 later corrected this drift and added a real-path regression test.
- All Phase 4A.0 security findings are isolated and deferred in `docs/data-query-agent-deferred-security-register.md`; no remediation or Supabase change was made.
- The Phase 4A.0 map, live table coverage, question taxonomy, promotion order, and acceptance gates are in `docs/data-query-agent-phase4a-capability-map.md`.
- Data Query Phase 4A.1 implements typed `lookup_latest`, `lookup_earliest`, and `lookup_last_n`, deterministic stable-ID ordering, bounded `machineResult.recordsByRequestId`, bilingual target-aware parsing, semantic precedence, exact-response contract checks, and workflow question/record redaction.
- Lookup routing is fail-closed: Main and Data Query do not execute a recognized lookup unless its exact target has an enabled typed policy. Financial, safety, alert, meeting, email, and exception lookups are now credential-gated reviewed policies; `data_index` lookup and every table from Phase 4G onward remain dormant.
- Phase 4A.1 fixes the missing-selection fallback to exactly `data_index` and removes canonical raw PostgREST `select` lookup execution. All 40 focused Data Query tests pass; no Supabase query or change was performed.
- Phase 4A.1 manual UI regression passed: exact indexed count used Data Query; latest invoice stayed on `financial_transactions`; latest meeting stayed on `meetings`; and the invoice-rejection explanation used financial/email retrieval without Data Query. The user verified the latest invoice and meeting against their source tables. Evidence and the separate 73k-137k-token/timeout findings are in `docs/data-query-agent-phase4a1-manual-regression.md`.
- Data Query Phase 4A.2 registers a fixed `financial_transactions` policy without activating it. The canonical date is `transaction_date`; lookup ties use `id`; and invoice means exactly `transaction_type = 'חשבונית'`.
- The financial operation allowlist is count, group count, distinct, time series, and bounded latest/earliest/last-N. Numeric money fields remain non-queryable and `not_computable` because the Phase 4A.0 catalog snapshot found them unpopulated.
- Financial policy registration and deployment availability are separate. The local policy has no exact RPC, exposes no active exact operations, never falls back to raw PostgREST, and keeps Main invoice routing on the existing retrieval tool.
- Phase 4A.2 trusted fixtures cover the 27-item acceptance matrix; all 50 focused Data Query tests pass, and the full suite retains the same 11 unrelated UI/static-contract failures. Live trusted-SQL/RPC proof remains approval-gated. The repeatable UI checklist is `docs/data-query-agent-phase4a2-manual-regression.md`.
- The 2026-07-25 existing-interface audit found no live typed exact financial contract. `match_financial_transactions` is semantic/vector-only; `bidoc_data_query_data_index_v1` remains fixed to `data_index`.
- Managed-identity `HEAD financial_transactions?select=id&limit=1` returned HTTP 206 with 100 rows, confirming SEC-001 raw-table reachability. This is an over-broad `authenticated` privilege, not an approved Data Query interface; do not activate a direct PostgREST adapter under the current contract.
- The user later clarified that structured table reads are allowed while every database/table mutation remains forbidden. Phase 4A.3 therefore supersedes the earlier no-direct-read conclusion for one reviewed agent adapter.
- Phase 4A.3 activates `financial_transactions` only when dedicated Data Query credentials exist. The transport is hardcoded to the Content table, accepts only validated typed plans, emits only `GET`/`HEAD`, has no request body or raw SQL path, and rejects any renamed table before network execution.
- Financial counts, group counts, distinct values, time series, and bounded latest/earliest/last-N lookups are active. Invoice still means exactly `transaction_type = 'חשבונית'`; semantic explanations stay on retrieval and money aggregates remain `not_computable`.
- Live managed-identity verification passed latest invoice, invoice count, and invoice grouping by status as exact over 23 matching rows with no warnings. All 52 focused Data Query tests pass; the full suite retains the same 11 unrelated UI/static-contract failures.
- No SQL, RPC, migration, permission, RLS, role, schema, table, row, saved selection, or Supabase setting was changed. SEC-001 remains deferred because agent-code restrictions do not narrow the identity's broader native privileges.
- Phase 4A.3 authenticated UI regression found that a persisted `data_index`-only picker selection excluded the credential-gated built-in financial policy from runtime routing. Runtime settings now preserve that saved selection while merging only the reviewed active `financial_transactions` policy. Exact structured routes use deterministic planning, schedule Data Query before semantic enrichment, and skip Hybrid Search, graph search, and reranking. The corrected UI run returned the exact 2026-02-28 invoice and completed Main synthesis with 18,410 tokens; all 54 focused Data Query tests pass. No database or saved-setting mutation occurred.
- Exact latest-invoice answers now add deterministic same-ID enrichment after Main synthesis: authoritative Data Query date/supplier/status/type/currency plus available amount/category/topic/summary and a validated HTTP(S) `data_link` from the matching financial row. If semantic results omit the exact row, one fixed GET by the exact integer ID supplies the display fields. Mismatched IDs and unsafe URLs fail closed. Live UI returned `SI266000183`, 802.40 ILS and its description; the row had no usable `data_link`, so no URL was invented. All 57 focused Data Query tests pass; no database or saved-setting mutation occurred.
- Exact invoice metrics and bounded lookups now carry the mandatory invoice discriminator through routing, deterministic planning, and validation. These routes schedule only Data Query, skip semantic financial retrieval and Main generation, render machine metrics/records deterministically, and enrich bounded lists with one GET-only exact-ID batch. Status totals can no longer include other financial transaction types, date-count answers cannot add semantic examples, and genuine multi-source conflicts remain visible on routes that still combine sources. All 61 focused Data Query tests pass; the full suite retains the same 11 unrelated UI/static-contract failures. At that historical checkpoint, fresh UI review was still pending; later entries record completion.
- Whole-table financial-document metrics now treat `financial documents`, `financial records`, and `מסמכים פיננסיים` as every row in `financial_transactions`, while invoices remain the exact `transaction_type = 'חשבונית'` subset and generic unqualified documents remain `data_index`. These routes reject a transaction-type narrowing filter, normalize classifier midnight timestamps to inclusive calendar dates, schedule only Data Query, render exact counts or `transaction_type` breakdowns without an unsolicited amount, and skip semantic retrieval/Main generation. The explicit whole-table phrase takes precedence even when invoices are mentioned as an example. All 64 focused Data Query tests pass; the full suite retains the same 11 unrelated UI/static-contract failures. Fresh UI review is the checkpoint gate.
- Hebrew exact-invoice routing now recognizes the complete approved wording `מה עלה בחשבונית האחרונה?` without widening the attached `ב` to meeting questions, plural invoices, earliest wording, line-item requests, or trailing qualifiers. Neighboring unsupported invoice-content forms fail closed even when the classifier incorrectly hints Data Query. The exact route keeps semantic precedence, applies the invoice discriminator, bypasses generic retrieval, schedules only Data Query, and uses deterministic same-ID enrichment/formatting. All 65 focused Data Query tests pass; fresh UI review is the checkpoint gate.
- Exact singular latest-invoice enrichment now resolves a missing direct document URL through the existing composite relationship `financial_transactions.email_attachment_id` + the exact row's validated `project_id` -> `email_attachments.attachment_id` + `project_id`. The fixed GET accepts exactly one matching row, preserves a safe direct `data_link` as first priority, rejects ambiguous/mismatched/unsafe results, and never removes invoice details on attachment failure. Supplied caller project scope must match; the current unscoped localhost chat remains compatible through the exact source row's project. All 68 focused Data Query tests pass; the full suite retains the same 11 unrelated UI/static-contract failures. Fresh UI review is the checkpoint gate, and no database or Supabase state changed.
- The exact latest-invoice UI now hides the long Outlook destination behind the inline `פתיחת מסמך החשבונית` label while preserving the separate filename source chip and the exact shared destination. The formatter recognizes angle-bracket Markdown links before escaping, validates HTTP(S) links, rejects credentials/control characters, escapes labels and hrefs independently, and preserves `_blank` plus `noopener noreferrer`. The authenticated localhost UI passed with record 226, one clean inline link, and the `SI266000183.pdf` source chip. All 69 focused Data Query tests pass; the full suite retains the same 11 unrelated UI/static-contract failures. No Content financial table, schema, permission, RLS policy, query contract, or Supabase setting changed; the verification created only normal chat/run history.
- Exact bounded last-N invoice answers now reuse a capped attachment-link resolver without widening table access: safe direct links win, missing links use only the exact attachment-plus-project reference, duplicate references share a read, failures stay per item, the 25-row cap and four-read concurrency ceiling fail closed, and workflow telemetry contains aggregate counters only. The authenticated five-invoice UI gate preserved all five records and rendered five inline links; five exact references resolved to three distinct destinations, so source chips were correctly ordered and deduplicated to three. All 71 focused Data Query tests pass, and the full suite retains the same 11 unrelated UI/static-contract failures. No financial data or database/Supabase configuration changed, and all other unreviewed tables remain explicitly deferred.
- The 2026-07-28 financial transaction-type vocabulary maps all 19 audited raw `transaction_type` values to 18 canonical concepts. Every reviewed Hebrew/English alias has an exact `eq`/`in` filter; partial accounts combine `חשבון חלקי` with the stored typo `חשבבון חלקי`, while ambiguous additional-work/cost wording requires an explicit financial qualifier. Complete type lists use a separate 200-row bound, exact-ID enrichment is batched in groups of 25, and response-only canonicalization merges the typo in lists and whole-table type breakdowns. Semantic content/insight requests remain retrieval-routed. The executable map is `src/subagents/dataQueryFinancialLexicon.js`, the UI matrix is `docs/data-query-financial-transaction-type-vocabulary.md`, and 120/120 protected Data Query tests pass. No database or Supabase state changed.
- Phase 4B promotes only `public.safety_reports` into the canonical typed Data Query runtime. It supports exact counts and canonical risk grouping, approved report-recorded defect sums, bounded metadata lookups, inclusive date scopes, and deterministic exact answers. Cross-report worker aggregation and report-level resolved/unresolved questions fail closed as `not_computable`; semantic descriptions stay retrieval-routed. Safety links require an authorization-bound caller project scope and remain hidden for the unscoped localhost UI. Exact reads use the dedicated Data Query bearer, and workflow telemetry omits raw values, URLs, UUIDs, record/attachment IDs, filenames, and plan/request IDs. All 80 focused Data Query tests and all 13 authenticated UI checks pass; no database or saved-selection state changed.
- The remaining one-table promotion roadmap is formalized in `docs/data-query-agent-phases4c-4k-roadmap.md`: 4C alerts, 4D meetings, 4E emails, and 4F exceptions are complete; 4G WhatsApp analysis is the next unauthorized approval gate, followed by zero-row readiness-gated 4H-4K. Every table requires separate `.1`, `.2`, and `.3` approval checkpoints.
- Phase 4C promotes fixed `public.alerts` into the canonical typed Data Query runtime. Its 9/9 groups, 89/89 then-protected tests, and all authenticated checks passed. The statement that Phase 4D had not started is historical to the Phase 4C checkpoint.
- Phase 4D promotes fixed `public.meetings` metadata. Exact routes use Data Query only; pure semantic routes use Meeting Evidence only; the approved mixed route runs Data Query first and then exact same-meeting evidence. Subject, attendance, decisions, lifecycle inference, and exact links remain excluded. The structural RPC compatibility fallback is bodyless, fixed-table, capped at 500, single-project-only when unscoped, validation-heavy, non-adjacent, and redacted. Verification passed 10/10 Phase 4D groups, 99/99 protected tests, and 17/17 authenticated UI cases. No Content/database/settings mutation occurred; production/multi-project scope and SEC-001 remain deferred. Phase 4E and Phase 4F are complete; Phase 4G is the next unauthorized approval gate.
- Phase 4E email relevance scope is explicit and fail-closed. Ordinary exact, lookup, mixed, and semantic email work uses only `project_related|multi_project`. Explicit bilingual unclear/non-project requests may count `no_clear_project` rows but cannot group, retrieve, display, or interpret them, and spam is never inferred from that value. Timeline-impact email questions route directly to the filtered internal `emails` tool even when the classifier suggests generic retrieval.
- Phase 4F promotes fixed `public.exceptions_report` metadata. It supports exact total/dated/undated/date-scoped counts, stored urgency or item-status groups, day/month series with an undated bucket, and dated bounded lookups. Exception numbers, identities, companies, amounts, execution days, links, and lifecycle interpretation remain excluded or `not_computable`. The approved mixed latest-exception route runs Data Query first and then admits only exact same-record evidence attested by exception/project/attachment identifiers; identifiers remain internal and redacted. Verification passed 7/7 Phase 4F groups, 114/114 protected Data Query tests, and the authenticated exact/lookup/mixed/fail-closed UI matrix. No Content/database/settings/production mutation occurred; Phase 4G remains unauthorized.

## Recent Changes

- 2026-08-17 -- Completed and published the explicitly approved Contracts Relationships R4.2A persistence/review slice to KAPAIM as migration `20260817100230_contracts_relationships_r4_2a_review`. Complete R4.1 proposals can now be persisted idempotently in the existing append-only `private.contract_relationships` table and reviewed in Hebrew by approve, reject, or bounded type/direction correction; corrections preserve a superseded model revision and a linked human-origin row. A live rollback-only smoke test proved insert, rerun reuse, approval, correction, and zero normalized-decision writes. Verification passed 132/132 Contracts tests, 47/47 Schedule tests, syntax checks, and the React build. R4.2B normalized decisions, conflict adjudication, and all Schedule integration remain separate approval gates.
- 2026-08-16 -- Precision-hardened Contracts Relationships R4.1 after the user's visual review exposed an over-permissive 43/48 single-pass result and a questionable `12.3 -> 11.8` amendment. R4.1 v2 now rejects amendment claims without explicit source amendment language and sends every surviving fixed type/direction to a separate skeptical verifier that can only accept or reject. The retained-contract run reduced 34 classifier proposals to eight final review proposals, rejected 24 at verification plus one asymmetric-party false conflict, retained the genuine 6.7 versus Appendix B.3 penalty conflict, and produced no surviving amendment, duplicate, or broad same-decision grouping. Verification passed 127/127 Contracts tests, 47/47 Schedule tests, and the React build; persistence, decisions, R4.2, and Schedule writes remain gated.
- 2026-08-16 -- Implemented the user-approved Contracts Relationships Agent R4.1 semantic preview over the retained saved clause generation. Candidate retrieval uses only R3.3 operative records, model classifications require a 90% gate and Hebrew source grounding, and an actor-aware safeguard rejects asymmetric-party false conflicts. The final read-only KAPAIM/OpenRouter run assessed 48 pairs, retained the genuine clause 6.7 versus Appendix B.3 penalty conflict, rejected the false 19.6 versus 19.7 offset-right conflict, and produced zero decisions, persistence writes, or Schedule writes. Verification passed 125/125 Contracts tests, 47/47 Schedule tests, and the React build. Model-proposal persistence, review decisions, normalized contractual decisions, and Schedule integration remain separately gated.
- 2026-08-15 -- After the user's visual acceptance of R3.3, implemented the bounded Contracts Relationships Agent R4.0 explicit-reference foundation locally. The Hebrew Contracts tab deterministically shows the retained workspace's 15 explicit references as 14 canonical clause-to-clause proposals plus one unresolved target; it makes no model conclusion and creates no decision or Schedule row. The additive service-role-only migration reuses the R1 append-only relationship table, serializes same-workspace policy runs, and reuses identical revision-1 proposals. Verification passed 121/121 Contracts tests, the React build, a dedicated local Supabase insert/reuse/rollback fixture, and authenticated Chrome rendering with 14 cards, one unresolved section, and no console warnings. Remote KAPAIM migration apply, activation, relationship writes, and later semantic/model-based R4 slices remain separately gated.
- 2026-08-15 -- Implemented Contracts Pipeline R3.3 as a read-only Hebrew presentation and structural-classification layer over the retained R3.2 generation. The authenticated Contracts tab now distinguishes 25 structural headings, 152 operative clauses, 2 definitions, and 10 context records across all 189 saved records; all canonical tags have locked Hebrew display labels; headings render as dividers with preserved source lead-ins; and machine keys/hashes are collapsed under technical details. Verification passed 118/118 Contracts tests, the React build, a read-only 743/743 KAPAIM projection, and authenticated Chrome interaction with no console warnings/errors. The saved KAPAIM rows were not rewritten, no model or Schedule call ran, and R4 remains unstarted pending user visual acceptance.
- 2026-08-15 -- Completed the locally verified Contracts Pipeline R3.2 durable clause-persistence package. It reuses the R1 private workspace/clause tables, R3 enrichment state, and the existing private content-addressed PDF bucket; adds one bounded atomic service-role batch RPC plus list/find/get projections; reopens complete clause views without a parser, model, PDF upload, or Schedule call; and keeps classic Phase 3F.1 workspaces isolated. Local tests passed with exact insert/reuse counts, populated rollback refusal, browser-role denial, and zero decision, relationship, or Schedule rows. Remote KAPAIM apply and activation remain separately gated; R4 has not started.
- 2026-08-15 -- Implemented the separately approved Contracts Pipeline R3.1 no-write visual acceptance slice in the existing Contracts tab. An authenticated bounded preview route runs the accepted parser and enrichment pipeline without persistence, while the UI exposes all clause evidence, Hebrew summaries, controlled tags, explicit-reference observations, search content, coverage metrics, filters, and a same-document comparison with the classic Contracts extraction. R4 Contracts Relationships Agent work remains unapproved and has not started.
- 2026-08-15 -- Completed the separately approved local Contracts Pipeline R2 Contracts clause parser: immutable PDF and parser-generation identity, complete numbered-clause and appendix extraction with cross-page continuation, a fail-closed coverage ledger, and exact R1 RPC payloads. The approved 18-page Herzliya fixture accounted for 743/743 source lines, stored 189 logical records including 8 cross-page clauses, and passed two-generation local Docker persistence with zero decision, relationship, Schedule, model-call, or remote-write activity.
- 2026-08-15 -- Closed the separately approved Contracts Pipeline R3 clause enrichment/indexing phase. The approved 18-page Herzliya fixture enriched 189 clauses in each of two isolated persistence generations, extracted 15 explicit-reference observations (14 resolved and one genuine unresolved Appendix C reference), attested 378 shared-index references, reused all 189 same-generation rows with zero repeat model calls, and left decisions, relationships, Schedule tables, remote systems, and source evidence unchanged. The final bounded `openai/gpt-4o` run used provider-enforced strict JSON Schema plus local validation, summarized and tagged 189/189 clauses in 24/24 successful calls with zero repairs or duplicate summaries, and passed a 19-clause source/summary review. R4 remains unapproved.
- 2026-05-09 -- Alert agent calls from chat now pass structured date bounds separately from the human-readable date filter.
- 2026-05-09 -- Alert endpoint accepts `date_from` and `date_to` for direct subagent calls.
- 2026-05-10 -- Added dedicated Alert Agent visibility to the workflow graph and inspector.
- 2026-06-23 -- Added the BIDoc Principal Database Architect prompt specification.
- 2026-06-23 -- Added the Meeting Evidence Agent specification for citation-backed hybrid retrieval from `meetings_documents`.
- 2026-06-24 -- Added the contractor delay-claim agent roadmap with four staged Codex implementation prompts.
- 2026-06-24 -- Added the Delay Claim Stage 2 subagent implementation and analyze API integration.
- 2026-06-24 -- Added Delay Claim Stage 3 event deep-analysis agents, workflow nodes, finding persistence, readiness scoring, and UI integration.
- 2026-06-24 -- Added Delay Claim Stage 4 package agents, schedule/cost/export persistence, dashboard UI, and Supabase migration.
- 2026-06-26 -- Added the Data Query Agent Codex prompt for safe multi-table read-only query planning and execution.
- 2026-06-26 -- Implemented the Data Query Agent with a read-only allowlisted executor, direct API endpoint, Subagents UI card, workflow node, Main Agent integration, and focused validator/executor tests.
- 2026-06-26 -- Added the Data Query Agent LLM planner with JSON-only prompt, planner settings UI, OpenRouter workflow telemetry, unsafe-plan audit warnings, and fallback to the deterministic planner.

- 2026-07-22 -- Added a current-state source map, risk audit, test-gap analysis, and phased hardening roadmap for the Data Query Agent.
- 2026-07-22 -- Completed Data Query Phase 0/1 baseline: canonical Content-only Query Plan, strict direct-API secret, dedicated database-role token, total deadline, redacted workflow details, retired SQL/schema routes and UI, and a live Kapaim migration that removed `exec_read_sql` and provisioned `bidoc_data_query` with `SELECT` only on `public.data_index`.
- 2026-07-22 -- Completed Data Query Phase 2 correctness hardening: typed field policy, exact fixed-table analytics RPC, explicit exactness/cardinality/provenance, planner-inclusive deadline, 25 focused tests, trusted-SQL comparison over 1,248 live rows, and a rolled-back 10,000-row gold fixture.
- 2026-07-23 -- Completed Data Query Phase 3 routing/reuse contract: `data-query.v2`, normalized caller envelope, narrowing budgets, enforced date/project scope, semantic route-away, same-run dedup cache, machine metric mapping, workflow caller metadata, UI cache controls, and 31 focused tests.
- 2026-07-23 -- Applied Data Query Phase 3.1 managed-auth hardening to Kapaim: short-lived Supabase Auth service tokens, immutable service claim, no raw `data_index` grant, guarded exact wrapper, inaccessible implementation function, 35 focused tests, and positive 1,248-row/negative 42501 live proofs. Service-account provisioning and the HTTP smoke test remain.
- 2026-07-23 -- Phase 3.1 provisioning audit found the local Content connection falling back to MAIN. The newly created wrong-project Auth user was deleted and verified absent; provisioning/runtime now reject App fallback, 36 focused tests pass, and explicit Kapaim Content credentials are the continuation gate.
- 2026-07-23 -- Completed local Phase 3.1 acceptance against Kapaim: confirmed managed Auth identity and immutable marker, successful live LLM-planned exact count of 1,248, same-run cache hit, HTTP 401 API-secret negatives, semantic route-away, forbidden-table rejection, and 36 passing Data Query tests. Deployment propagation and production smoke remain.
- 2026-07-24 -- Completed read-only Data Query Phase 4A.0 discovery: live structured-table capability matrix, Data Query/retrieval routing taxonomy, typed lookup design, financial-first promotion roadmap, source-index/data-quality checks, fallback-manifest drift, managed-token privilege correction, and separate RLS-disabled-table security finding. No Supabase or runtime state changed.
- 2026-07-24 -- Completed local Data Query Phase 4A.1 typed-lookup foundation: target-aware bilingual lookup parsing, strict operation/table/order/limit validation, deterministic stable-ID ties, bounded machine records, exact-RPC contract enforcement, redacted workflow history, dormant-policy routing gate, `data_index`-only fallback, and 40 focused passing tests. No Supabase query or change was performed.
- 2026-07-24 -- Completed the Phase 4A.1 manual UI regression gate: four route/answer checks passed, the user confirmed the latest invoice and meeting in their source tables, and Main Agent token/timeout inefficiency was recorded separately from Data Query correctness.
- 2026-07-25 -- Completed local Data Query Phase 4A.2: dormant typed financial policy, exact Hebrew invoice discriminator, explicit operation/field/date allowlists, money fail-closure, 50 focused passing tests, unchanged Main/retrieval routing, and no Supabase query or mutation. The full suite kept the same 11 unrelated UI/static-contract failures. Live RPC design, deployment, trusted-SQL comparison, and production activation remain deferred.
- 2026-07-25 -- Completed a read-only existing-interface activation audit: managed auth and OpenAPI succeeded; only semantic financial matching exists; direct raw financial-table access confirms the deferred SEC-001 privilege bypass. No row values, code route, settings, SQL, permission, or database object changed, and financial Data Query remains dormant.
- 2026-07-25 -- Completed Phase 4A.3 after the read-only boundary clarification: added the fixed credential-gated `financial_transactions` GET/HEAD adapter, preserved typed allowlists and semantic precedence, passed 52 focused tests and three exact live read-only checks over 23 invoices, and made no database or Supabase mutation.
- 2026-07-25 -- Corrected the Phase 4A.3 UI routing regression: preserved the persisted `data_index` picker while merging the reviewed active financial policy at runtime, added deterministic exact-route planning and retrieval/reranker bypass, improved synthesis fallback, passed 54 focused tests, and verified the exact latest invoice through the authenticated UI without database or settings mutation.
- 2026-07-25 -- Added deterministic same-ID latest-invoice answer enrichment in the Main Agent layer, including a fixed GET-by-exact-ID fallback, available financial details, and a validated document link when the source row provides one; live UI passed, the tested row had no usable link, mismatches and unsafe URLs fail closed, and 57 focused Data Query tests pass.
- 2026-07-25 -- Corrected deterministic invoice metrics and bounded lists: mandatory invoice filters for every exact operation, Data Query-only scheduling, direct machine-result formatting, exact-ID batch enrichment, and visible conflict warnings. Focused verification is 61 passing tests; fresh UI examination is the checkpoint gate.
- 2026-07-25 -- Corrected whole-table financial-document metrics: compound bilingual routing to all `financial_transactions` rows, no transaction-type narrowing, inclusive classifier date bounds, Data Query-only scheduling, exact total/type-breakdown formatting, co-occurrence precedence, and 64 focused passing tests with no database or Supabase mutation.
- 2026-07-25 -- Corrected the approved Hebrew wording `מה עלה בחשבונית האחרונה?`: exact latest-invoice lookup, terminal phrase scope, preserved semantic and qualified-query negatives, Data Query-only scheduling, and 65 focused passing tests with no database or Supabase mutation.
- 2026-07-25 -- Added fail-closed exact latest-invoice attachment-link resolution through the existing attachment ID plus project relationship, with safe direct-link precedence, 68 focused passing tests, unchanged 11 unrelated full-suite failures, and no database or Supabase mutation.
- 2026-07-25 -- Cleaned up exact latest-invoice link presentation: one safe inline document label, unchanged filename source chip and destination, cache-busted frontend loading, authenticated UI verification, 69 focused passing tests, and no Content financial-table or Supabase-setting mutation.
- 2026-07-25 -- Extended exact attachment-link resolution to bounded last-N invoice lists with per-item fail closure, exact composite deduplication, a 25-row cap, four-read concurrency ceiling, redacted aggregate telemetry, ordered/deduplicated source chips, 71 focused passing tests, and a successful authenticated five-invoice UI gate. All other unreviewed tables remain disabled pending explicit policies and tests.
- 2026-07-26 -- Completed the read-only Phase 4B `safety_reports` promotion: audited 21 live rows, added the fixed typed GET/HEAD policy and canonical risk/defect/lookup semantics, preserved semantic and mixed routing, made worker/resolution aggregates fail closed, required authorized project scope for links, redacted workflow identifiers, passed 80 focused tests, and completed all 13 authenticated UI checks without database or saved-selection mutation.
- 2026-07-26 -- Formalized the documentation-only remaining Data Query roadmap as one table per Phase 4C-4K, with uniform audit, implementation/test, and authenticated UI closeout checkpoints, explicit security/privacy gates, zero-row readiness blockers, and non-promotion dispositions. At that checkpoint no Phase 4C work or database/runtime change had started.
- 2026-07-26 -- Completed Phase 4C for fixed `public.alerts`; at that checkpoint Phase 4D had not started.
- 2026-07-26 -- Completed Phase 4D for fixed `public.meetings`: audited 151 metadata rows and the sparse exact evidence relationship, implemented exact/semantic/sequential-mixed routing plus the structural RPC compatibility fallback, passed 10/10 Phase 4D groups, 99/99 protected tests, and 17/17 authenticated UI cases, and made no Content/database/settings mutation. Phase 4E remains unstarted; SEC-001 and production/multi-project scope remain deferred.

- 2026-07-27 -- Completed Phase 4E for fixed `public.emails`: audited 7,163 source rows and fixed ordinary scope to 786 project-related rows, added PII-safe received-date/category/direction/attachment-flag metadata analytics and bounded lookups, enforced the relevance predicate in validation and transport, preserved semantic/mixed content routing, passed 6/6 Phase 4E groups and 106/106 protected tests, and passed the authenticated exact/semantic/mixed/redaction UI matrix. Exact answers expose no PII or internals; relevance-ranked latest-content answers carry a deterministic non-exact boundary. No Content/database/settings mutation occurred. Phase 4F has not started.
- 2026-07-27 -- Reopened and reclosed Phase 4E.3 after user-discovered Hebrew grammar gaps: `כמה מיילים יש במערכת?` now returns the exact scoped 786 count, and `מה המייל האחרון שמופיע?` now returns the exact 31.03.2026 safe metadata instead of entering semantic retrieval. Both authenticated reruns skipped hybrid/graph/reranker and Main, used Data Query only, completed in roughly five seconds, and are covered inside the still-green 6/6 Phase 4E and 106/106 protected suites. No metadata, privacy, Content-data, or database-setting boundary changed.
- 2026-07-27 -- Reopened and reclosed Phase 4E relevance filtering: explicit relevant counts remain scoped to 786 `project_related|multi_project` rows, explicit unclear/non-project counts return 6,377 `no_clear_project` rows through a separately attested count-only predicate, spam fails closed, and timeline-impact questions force the internal relevant-only email tool. Six Phase 4E groups and 106/106 protected Data Query tests pass; authenticated UI verified all four routes. Main synthesis latency on the timeline question remains separate from the correct filtered retrieval.
- 2026-07-28 -- Reopened the Phase 4E Hebrew grammar checkpoint for `כמה מיילים שייכים לפרויקט יש מתוך כל המיילים?`: project-membership wording and the harmless comparative tail now normalize to the existing fixed project-related count without adding an all-mail denominator or widening relevance/privacy scope. The exact regression, 6/6 Phase 4E groups, and 106/106 protected tests pass; the restarted live pipeline returned 786 in about five seconds with retrieval and Main skipped. The Chrome bridge exposed but could not control the user's open authenticated tab, so this exact browser rendering remains a manual refresh check rather than claimed UI evidence.
- 2026-07-28 -- Replaced scattered Phase 4E Hebrew email-relevance aliases with a centralized controlled lexicon covering spelling variants, gender/number morphology, project-association synonyms, explicit negative/unclear forms, and common email/count aliases. A 15-positive/13-negative matrix plus fail-closed controls passes inside 7/7 Phase 4E and 107/107 protected Data Query tests; the full suite is 352 passed with the same 11 unrelated failures. Live chat returned 786 for `רלוונטים` and 6,377 for `לא שייכים לפרויקט`, with deterministic Data Query only. Relevance predicates and all database/Supabase state remain unchanged; other-table Hebrew vocabulary is still approval-gated.
- 2026-07-28 -- Completed Phase 4F for fixed `public.exceptions_report`: audited 20 source rows, enabled only date/urgency/item-status exact metadata, kept exception numbers, identities, companies, money, execution days, links, and lifecycle interpretation excluded or `not_computable`, and added an exact same-record evidence handoff with exception/project/attachment attestation. Verification passed 7/7 Phase 4F groups, 114/114 protected Data Query tests, and authenticated exact/lookup/mixed/fail-closed UI checks. No Content data, schema, role, grant, RLS, Auth/Supabase setting, production configuration, or deployment changed; Phase 4G is the next unauthorized gate.
- 2026-07-28 -- Added the controlled financial `transaction_type` vocabulary for all 19 live raw values / 18 canonical concepts, exact type-specific counts and complete lists, response-only typo canonicalization, 200-row complete-list and batched enrichment bounds, semantic precedence, ambiguity fail closure, and a published UI matrix. A follow-up regression found that one live process could bypass retrieval using one capability snapshot but re-plan Data Query from another, then substitute semantic fragments after plan rejection. Main now reuses one request-scoped Data Query settings/routing snapshot across scheduling and execution, and exact financial failures stop before semantic synthesis. Verification is 121/121 protected Data Query tests; the full suite retains the same 11 unrelated UI/static-contract failures. No database, table, row, RLS, permission, role, Supabase setting, or deployment changed.
- 2026-08-01 -- Phase 4H completed through authenticated UI closeout for fixed `public.consultants_reports`: one dated report, 18 same-report chunks, canonical `report_date`, stored `item_status`, exact counts/groups/series/lookups, and report/project/attachment-attested mixed recommendations. The bilingual UI matrix passed exact, mixed, and fail-closed cases after closing people-count semantic fallthrough, Hebrew implementation morphology, and bidirectional ingestion-time grammar. Company names are allowed in semantic narrative; internal report/document identifiers remain redacted. Verification passes 1/1 focused and 123/123 protected Data Query tests; the full suite retains the same 11 unrelated UI failures. Phase 4I remains unauthorized.
- 2026-08-05 -- Repaired `מהו החריג האחרון?`: singular Hebrew exception grammar now selects exact Data Query, bypasses hybrid/graph/reranking, and schedules only `data_query`. The fixed read-only transport honors the explicit MAIN mapping, adapts to MAIN `exceptions_report` (no `urgency_level`, `DD/MM/YYYY` dates, stored statuses `בטיפול|rejected`), normalizes and sorts all dated rows server-side, and deterministically returns 14.08.2025 / `בטיפול`. Phase 4F is 9/9 green; the full suite retains the same 11 unrelated UI/static-contract failures. No database mutation occurred.

- 2026-08-06 -- Added 14 operational internal subagents to Tool Testing with separate read-only readiness checks, explicit inactive-state reporting for disabled agents or missing dependencies, and focused regression coverage.
- 2026-08-15 -- Activated the bounded Contracts Relationships Agent R4.0 foundation on KAPAIM: the reviewed service-role-only migration reuses `private.contract_relationships`, saved 14 deterministic explicit-reference links for the retained 189-clause workspace, preserved one unresolved Appendix C reference, and remained idempotent on rerun. Decision and Schedule counts stayed at zero; semantic/model grouping remains approval-gated.
- 2026-08-17 -- Published and runtime-hardened Contracts Decisions Agent R4.2B on KAPAIM. It reuses `private.contracts` for append-only normalized decision proposals and `private.contract_relationships` for exact clause-to-decision support, blocks generation until all R4.2A proposals are reviewed, exposes Hebrew approve/reject/correct/unresolved review, selects no conflict winner, and performs no Schedule write. After the user completed all 19 relationship reviews, a real provider run exposed four-item `MAX_TOKENS` truncation; production now uses two-item/three-call bounded batching, truncation telemetry and split fallback, omits ungrounded optional party labels, neutralizes unprovable temporal metadata, and replaces generated numeric drift with source-exact text. A full no-write Gemini 2.5 Pro run completed 135/135 decisions from 152 clauses in 187.43 seconds with 68 calls, 0 retries/repairs/truncations, 0 persistence writes, and 0 Schedule writes. Verification passes 137 Contracts tests, 47 Schedule tests, and the React build; retained live state remains 0 decisions and 0 pending relationship reviews until the user clicks the persisted generation action. Split/merge lineage review remains deferred to R4.2C.
- 2026-08-17 -- Completed and published Contracts Decisions Agent R4.2C on KAPAIM. It adds atomic append-only split and merge review over the existing `private.contracts` and `private.contract_relationships` tables, terminal source revisions, exact support-evidence conservation, directional `split_into`/`merged_into` lineage, strict current-revision concurrency checks, and persisted Hebrew lineage UI. Split allows evidence overlap but requires the exact source-evidence union; merge requires the exact union across all sources and preserves unresolved conflicts without selecting a winner. Browser roles cannot execute the three service-role-only RPCs, private-table RLS remains forced, rollback refuses to erase retained lineage, and the phase performs zero model calls and zero Schedule writes. A post-publication combined-screen regression found that the older R4.2A validator wrongly required its observed `decisionCount` to remain zero after R4.2B created 135 valid decisions; it now accepts a safe non-negative count while preserving every R4.2A relationship/evidence/version gate and the zero-Schedule invariant. Live KAPAIM revalidation returns 19/19 approved relationships, zero pending reviews, 135 decisions, and zero Schedule writes. Rollback-only split/merge smokes, managed KAPAIM publication, 141 Contracts tests, 47 Schedule tests, the React build, syntax checks, and `git diff --check` pass. The retained workspace remains at 135 current decisions and zero lineage links until the user saves a genuine reviewed correction. R5 shadow Schedule projection remains a separate approval gate.
- 2026-08-17 -- Implemented the bounded Contracts R5 shadow Schedule projection locally without publishing its migration. The authenticated GET-only planner reads current R4.2 decisions, classifies fixed/relative/extension targets, reuses the existing condition resolver and activity mapper, requires current human review plus matching decision-level Schedule mapping, exposes Hebrew blockers and exact shadow rows, and performs zero model, Schedule, mapping, runtime-date, or contractual-truth writes. A live read-only KAPAIM smoke over the retained workspace found 137 current records, 131 reviewed, 42 marked Schedule-impacting, six temporal target shapes, five reviewed target shapes, and zero eligible promotions because decision-level Schedule mappings are absent; the extension is additionally incomplete. The one-way nullable Schedule-to-`private.contracts` FK/guard migration, service-role-only decision-mapping read RPC, and refusal rollback are prepared but unapplied; until publication the UI uses an explicit legacy read fallback and keeps promotion closed. Verification passes 145 Contracts tests, 47 Schedule tests, the React build, syntax checks, `git diff --check`, and the live zero-write smoke. Restart the local server for UI acceptance; migration publication and one-target promotion remain separate approval gates.
- 2026-08-18 -- Corrected the R5 completeness gap that hid Schedule-impacting decisions with no target shape. The Hebrew Contracts UI now exposes all 37 retained rows with exact evidence, blockers, and deterministic non-mutating audit dispositions: 21 temporal/event-anchor correction candidates, two missing source values, 12 Schedule-context-only decisions, and two unresolved review/lineage rows. A reusable live audit command proves 42 Schedule-impacting decisions = five reviewed target-shaped rows plus 37 explained no-target rows, with zero model calls and zero writes. Read-only Supabase verification confirmed the three target tables still lack `source_contract_decision_id`, the R5 read RPC/migration are absent remotely, and migration history ends at R4.2C. Verification is now 146 Contracts tests plus the React build; R5 publication, canonical temporal corrections, and any promotion remain explicit live gates.
- 2026-08-18 -- Published the separately approved R5 one-way Schedule lineage migration to KAPAIM as remote version `20260817212254` (`contracts_schedule_projection_r5`, local SHA-256 `010E80CE45B57657FB361FAE161F1FD7A305FE559D7766E02E0E5CA0CBB2C024`). Post-publication verification confirmed three nullable origin columns, three `ON DELETE RESTRICT` foreign keys to current contractual decisions, three unique indexes, three cross-target validation triggers, the private validator, and the service-role-only `SECURITY INVOKER` read RPC. All Schedule target tables remain empty, the live retained-workspace audit now reports `migrationAvailable = true` with all 42 Schedule-impacting decisions accounted for, and every write counter remains zero. Contracts 146/146, Schedule 47/47, the React build, syntax checks, `git diff --check`, and advisor review pass. One reviewed append-only decision correction and one separately approved target promotion remain the R5 exit gates.
- 2026-08-18 -- Superseded the R5 Schedule-shadow/promotion interpretation after the user clarified ownership: Contracts only classifies decisions for a future `indicator_agent`; Indicator owns project placement, target selection/cardinality, activity mapping, date/calendar calculation, and every Schedule write. The active GET-only handoff derives from current `private.contracts` truth and creates no fourth table. Corrective KAPAIM migration `20260818080957` removes the legacy projection RPC and three Contracts-owned unique target indexes while retaining three ordinary lineage indexes and provenance-only triggers. The live retained workspace classifies all 137 current decisions as 40 suitable, 24 unsuitable, and 73 requiring explicit review (71 approved/corrected rows still have `schedule_impact = unknown`, two are non-terminal), with zero model calls and zero writes. Contracts 148/148, Schedule 47/47, React build, diff check, catalog verification, and advisor review pass. R5 implementation is complete; remaining suitability review changes only Contracts truth, while operational placement belongs to Indicator.

## Gotchas

- The Alert embeddings RPC currently receives only `query_embedding` and `match_count`; date bounds are applied after retrieval in local code.
- If no date range is classified, the chat-to-alert request leaves `dateFilter` empty and sends null date bounds.
- The canonical Data Query v2 runtime does not execute raw SQL or build joins; the LLM planner can only propose JSON plans that still must pass server-side validation before execution.

## Related

- See [chat](chat.md) for how the main agent routes tool calls.
