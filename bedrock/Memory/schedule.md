---
note_type: durable-memory-branch
project: bidoc agent
branch: schedule
last_updated: 2026-08-30
tags:
  - schedule
  - contract
  - rag
---

# Schedule Intelligence

## Current State

- `src/scheduleEngine.js` is the pure deterministic calculation layer; it receives normalized inputs and performs no I/O or LLM calls.
- The Schedule three-axis chart always includes every active contractual milestone date in its presentation scale, even when the default `רק באיחור` row filter excludes a future milestone. Unlinked milestone rows use `milestone:<milestoneKey>` as their stable React identity. Contract labels sit in a dedicated rail above the chart: saved trigger dates render as blue start-of-count markers, while final targets are green and incomplete-calendar provisional targets are orange/dotted. Trigger and target dates both participate in the chart scale.
- The CTO clarified on 2026-08-10 that the existing Schedule Engine and Calendar are a canonical baseline, not immutable files. Contracts and Indicator integration may modify or extend them when genuinely required, but must reuse the existing formulas, status/confidence/severity, basis, extensions, and calendar logic rather than duplicate it elsewhere. Every intentional behavior change requires focused regression evidence.
- `src/subagents/schedule.js` orchestrates source loading, calculation, snapshots, health, and schedule-alert lifecycle.
- `src/scheduleIngestion.js` is the only schedule table/profile-aware layer. Uploaded Gantt source data is read from MAIN; engine-owned `schedule_*` data is read and written through the existing `APP DATA` connection (`contentSource`) whose Supabase project is KAPAIM (`smxibuaowzuxkznuouwj`).
- Schedule has no separate URL, project ID, key, or Settings card. Direct KAPAIM introspection confirms that `gantt_files`/`gantt_tasks` exist there but are empty; the active upload source is therefore MAIN's populated `_test` tables.
- Gantt source ownership is project-specific. KAPAIM project `81b1cbac-8fcf-43c1-acdc-6b5c809de0e5` (`סמל - החושלים 15 הרצליה`) declares `gantt_files`/`gantt_tasks` in its `projects.settings`; MAIN project `652bf3e0-9a1e-47ca-b06f-cd8dc33907f7` (`פרוייקט כללי`) declares `gantt_files_test`/`gantt_tasks_test`. Engine-owned `schedule_*` data remains on APP DATA/KAPAIM.
- Gantt loading is project-aware: `loadScheduleSource` resolves the active project across APP DATA/KAPAIM and MAIN, validates the `projects.settings` table names, and queries that project's own database/tables. `listScheduleProjects` discovers active Schedule projects from both databases and includes projects with zero or more versions.
- Each Schedule project may persist `schedule_project_end_date` inside its existing `projects.settings`. When the UI's transient “as of” field is empty, this date becomes the deterministic calculation cutoff; an empty project-end date means an active project and falls back to today. The same-origin authenticated write preserves every other settings key. The chart separately offers presentation-only toggles for red lateness spans and the “as of” marker. Hiding “as of” also removes that date from the presentation scale and suppresses its dependent red spans, compacting the chart to the full schedule's first and last real dates while retaining contract, trigger, and activity-update markers.
- Manual contract-event dates use an explicit Israeli `DD/MM/YYYY` text contract in the Schedule UI, validate real calendar dates, and convert to ISO only at the resolver boundary. Saved trigger dates and their blue start-of-count marker labels format through the same Israeli-date helper, preventing browser-locale month/day reversal.
- The Schedule chart opens in a compact full-schedule default: axes view, late-only filtering off, red as-of lateness spans off, and the as-of marker off. Users may opt each overlay back in from the toolbar.
- Active Schedule alerts are grouped under one accessible collapsible header showing alert count and worst severity. Both this alerts group and the contractual relative-time ledger default to collapsed and expose semantic `aria-expanded`/controlled-region state.
- The agreed design for a controlled per-row alert-to-Gantt assignment agent is documented in `docs/Indicator + Contracts/BIDoc_Schedule_Activity_Assignment_Agent_Spec.md`. It locks one primary activity per alert, per-project configurable auto-assignment threshold, per-row-only invocation for V1, layered deterministic/semantic/temporal/hierarchy/RAG retrieval, role-separated models, a server-owned scoring/policy gate, audit persistence, and a dry-run-first rollout. The spec also defines a superadmin Settings workspace for versioned models, fallbacks, prompts, search tools, scoring weights, limits, global/project inheritance, no-write dry runs, publishing, diff, and rollback.
- The Schedule Activity Assignment Agent V1 is implemented locally. Each alert row has an explicit agent action; lexical/temporal/hierarchy scoring and bounded embeddings feed separate extractor, professional-matcher, schedule-validator, and conditional-judge roles. Server-owned gates require model agreement, threshold, runner-up margin, canonical date, active-version identity, no existing link, a fresh run, and audit-schema readiness before automatic assignment. Reviewer confirm/reject routes and a dedicated Settings workspace cover role models/prompts, search toggles, 100%-validated weights, thresholds, limits, and no-write dry runs. `schedule_activity_alert_links` remains the active-link truth; the unapplied migration adds server-only run/candidate audit tables and link provenance columns.
- The alert-to-activity table has a composable filter row for kind, business-date range, alert/update text, severity, status, assignment state, and assigned activity name, plus the existing global search and one reset action. The visible/result counter and assigned counter reflect the filtered set. Its explicit sequential batch controller queues only dated, currently unassigned alerts in that filtered set, awaits each existing per-row agent run before starting the next, continues past review/error outcomes, prevents parallel manual actions, and reports progress and assigned/review/failed counts. Stop is safe-after-current; once paused the user can resume at the first unprocessed row with preserved counters or restart from a fresh eligible queue. Project refresh/unmount invalidates the active controller token.
- The sequential controller has an optional, default-off `סינון זמן` checkbox that is never used by manual per-row runs. When enabled, a server-owned v1.1 pre-filter runs before Gantt loading and the full assignment pipeline: explicit time/schedule signals pass deterministically, otherwise the Settings-owned `timeFilter` role classifies the alert. Only a confident negative at or above `timeFilterConfidenceThreshold` (default 80) is skipped; missing credentials, disabled role, malformed output, low confidence, or model error fail open into the full pipeline. Skipped rows return `filtered_out`, show reason/confidence, and have a separate batch counter without creating candidates, an audit run, or a link.
- Human-review assignment results always expose up to two valid activity choices with score and planned range. Their visibility no longer depends on the audit migration: a persisted run uses the audited confirmation route, while an unavailable audit schema falls back to the existing explicit manual-assignment route. Successful manual assignment removes the stale agent review result.
- Human-review assignment results from new runs are also persisted in MAIN `schedule_activity_assignment_reviews` as a shared team queue. The server stores a bounded snapshot with at most two candidates, reloads pending reviews for the selected Schedule project, and marks every pending review for that source alert `selected` or `rejected` after a reviewer acts. The table is backend-only: RLS is enabled, browser roles have no table or RPC privileges, and only `service_role` can use its invoker RPCs. Existing `schedule_activity_alert_links` remains the assignment truth in KAPAIM; MAIN queue synchronization is deliberately non-atomic across the two databases and surfaces a warning if its secondary status update fails.
- MAIN migration `20260830174803 schedule_assignment_evaluation_labels` extends the shared review queue with six explicit evaluation-label fields and a service-role-only invoker RPC. The migration is live and catalog-verified, but no explicit label exists yet. A transactional `no_match` RPC smoke write was rolled back, leaving 4 queue rows: 3 pending, 1 superseded, and 0 labelled. The local collection endpoint reads all 3 pending cards and reports 0/100 explicit queue-label coverage. Application deployment uses the existing Vercel Git integration and does not require direct Vercel connector access; production verification remains required after Git publication.
- Every Schedule activity-assignment run now emits the shared Workflow contract and is registered as `schedule_activity_assignment`. Its graph covers trigger, alert/Gantt loading, optional time filter, audit initialization, extraction, deterministic and semantic candidate search, matcher, validator, conditional judge, policy gates, audit finalization, link write, and result. Nodes expose safe model/search/scoring parameters, candidate signals, trace events, and real OpenRouter telemetry; prompts are represented only by hashes and secrets are omitted. The unapplied audit migration now carries `workflow_log`/`run_events`, and `/api/run-history` merges these durable rows with local and chat runs; missing schema falls back to local history. Failed route executions still create an error workflow entry.
- The corrected KAPAIM upload contains one `לוז_בסיס_סמל_MS_Project` version and 102 tasks for project `81b1...`. A live application-loader verification returned exactly one file and 102 current tasks from APP DATA `gantt_files`/`gantt_tasks`; a read-only full sweep returned 102 indicators with zero warnings and data version `1787251318726_MS_Project.xml#v1`.
- Contract dates live in `schedule_contract_milestones`; relative obligations waiting for a trigger live in `schedule_contract_conditions`.
- Indicator-owned contract-condition synchronization is always active once its migration exists and has no environment feature flag. It accepts only current human-approved/corrected, conflict-free relative decisions with `scheduleImpact=yes` and an active project mapping; decision-revision IDs make the UPSERT idempotent, while superseded pending rows become `dismissed` and resolved history is preserved.
- Schedule sweeps reconcile contract conditions before operational resolution. Trigger lookup is structured-first (Gantt, then `schedule_observed_events`) and uses RAG only when no unambiguous structured source exists. Resolution is atomic through `bidoc_schedule_resolve_condition_v1`; the deterministic Schedule engine remains the only offset calculator.
- Working-day conditions may retain a verified trigger while remaining `pending` with `calendar_coverage_missing`; they cannot resolve until `holidays_through` covers the computed range.
- Contract source links are generated server-side as 60-second private Storage signed URLs after workspace/decision authorization. Schedule stores document identity and source metadata, never a public Storage URL.
- KAPAIM migrations `20260819135108` and `20260819135350` are live. The approved Herzliya workspace is synchronized idempotently into Schedule project `81b1cbac-8fcf-43c1-acdc-6b5c809de0e5` as four `pending` relative conditions: 100 working days, 15 calendar days, and two 12-month rules. All four currently have no trigger date or milestone.
- Unlinked active contract milestones still produce milestone-only indicators and visible global contract markers; they do not need an activity mapping to appear.
- `src/subagents/scheduleConditionResolver.js` resolves pending conditions row by row: it plans an event-date question, calls the complete chat RAG path ephemerally, verifies an ISO date plus quote, source URL, and confidence, then computes the due date deterministically.
- Resolver searches require `project_id_filter`; older hybrid RPCs that cannot enforce it fail closed instead of widening to tenant-wide retrieval.
- The auto-promotion threshold is 0.8. Ambiguous, uncited, unsupported, or low-confidence results remain pending for review.
- `calendar_days`, `weeks`, `months`, integral-day `hours`, and `working_days` are supported. Working-day resolution requires a project calendar; sub-day deadlines stay unresolved because the milestone schema stores a date, not a timestamp.
- `POST /api/schedule/conditions/resolve` accepts `conditionId` for exact single-row processing. The Schedule UI exposes one AI action per condition row; batch mode remains API-only and capped at 25.
- Internal resolver questions do not write chat history or conversational memory. The standard chat path is unchanged for normal callers.
- The resolver's OpenRouter credential is SETTINGS-owned: every row action refreshes MAIN `agent_settings`, reads `data.secrets.openRouterApiKey`, and explicitly forbids the legacy environment fallback.
- Schedule-page auxiliary loads are isolated: missing alert or contractual-condition tables become visible warnings and no longer discard a successful schedule sweep.
- The eight existing KAPAIM tables are canonical for Contracts/Schedule work: `schedule_calendars`, `schedule_contract_milestones`, `schedule_contract_extensions`, `schedule_contract_conditions`, `schedule_indicator_snapshots`, `schedule_alerts`, `schedule_activity_map`, and `schedule_observed_events`. A 2026-08-08 read-only OpenAPI/HEAD audit reconfirmed all eight without DDL or data writes.
- Schedule activity updates reuse the exact `fetchAlertsTimelineEvents` adapter that feeds the Timeline page, with KAPAIM `alerts` remaining the immutable source. User-reviewed one-to-one links live separately in service-role-only `schedule_activity_alert_links`; the Schedule page provides a searchable activity picker and expands assigned items as dated subrows/points under the activity.
- Contracts Agent Phase 1 is accepted as a local authenticated dry run with 32 Phase 1-focused tests and six exact canonical representative cases. The final 172.1-second real-contract report passes every unchanged hard gate and quality threshold, with zero approved Schedule projections and zero false operational eligibility. Phase 1 performs no Schedule write or arithmetic.
- Contracts Phase 2 is implemented and verified through the pure planner, `src/contracts/promotionWriter.js`, private audit/mapping tables, atomic RPC, authenticated routes, reviewer UI, and rollback plan. The migrations, security verification, approved mapping, and rejection-only live audit closure are complete; a real target-row promotion waits for genuinely eligible input.
- Contracts Phase 2 separates complete rejection-only review persistence from operational promotion. `/api/contracts/review/save` accepts only an all-rejected reviewed batch and writes immutable audit evidence with zero Schedule target rows; `/api/contracts/review/commit` accepts only a transaction-ready promotion. The reviewed Herzliya closure is now persisted in KAPAIM as one `review_only` batch with 12 rejected decisions and one `reviewed_no_promotion` attempt; all three mapped Schedule targets remain at zero. The temporary local activation process was stopped after verification.
- Contracts Phase 3B through Phase 3F are complete through schema-only apply. Phase 3F adds strict same-origin history and manual confirm/reject/correct/unmapped routes plus a Hebrew review workspace that shows exact evidence, current alternatives, conflicts, version, reasons, and immutable correction history. KAPAIM records the history migration as `20260812200241`; the exact write gate remains closed, and KAPAIM still has no activity mapping/review event from this workflow.
- Contracts Phase 3F.1 is activated and live-verified. Exact reuse binds MAIN project, KAPAIM Schedule project, PDF hash, and extraction fingerprint; KAPAIM records remote migration `20260812152042`, and `contracts-private` is private, PDF-only, and capped at 3,000,000 bytes. One exact-PDF workspace and revision-1 draft with 12 candidates exist; reopen and a byte-identical upload restored the Hebrew draft with zero additional model calls. Credential rotation remains recommended unresolved risk accepted by the user. No mapping, Schedule target row, arithmetic, or alert was created by this checkpoint.
- Phase 3G הושלם עד גבול schema-only: KAPAIM מתעד את ה־forward guard כ־`20260812200652`, קטלוג האבטחה נקי, ומיפויים/אירועי ביקורת נשארו `0`. PID `60996` פועל עם Phase 3F.1 פעיל ושער 3G סגור. preview חי עבור MAIN `652bf3e0-9a1e-47ca-b06f-cd8dc33907f7` נכשל סגור ב־409 `previous_version_missing`; apply נדחה ב־503 `not_approved`. לא נכתבו מיפוי, היסטוריה, שורת Schedule או התראה. הדרישה הבאה היא גרסת MAIN שנייה אמיתית ואז preview חוזר; apply חי דורש אישור נפרד.

## Recent Changes

- 2026-08-31 -- Clarified shared Schedule review cards by making the source alert the primary header, separating the model's leading recommendation, and labeling the two candidate buttons explicitly. The authenticated local page rendered the corrected hierarchy; Schedule tests pass 114/114 and the React bundle builds successfully.
- 2026-08-30 -- Activated the approved Phase 3.1 evidence schema on MAIN as migration `20260830174803`. Catalog verification confirms all six columns, three validated constraints, a valid partial label index, RLS, no browser table grants, an invoker RPC with fixed empty search path, and execute limited to `service_role`. A real transactional `no_match` resolution passed and rolled back, preserving 4 rows and 0 labels. The post-migration dataset refresh remains at 30 cases with 70 required and four missing classes; the local exact review API returned 3 pending cards. Deployment is driven by the tracked Git branch through Vercel's existing integration. No label, Schedule link, policy setting, calibrator, or automatic assignment changed.
- 2026-08-30 -- Implemented the Schedule activity-assignment Phase 4 policy-evaluation framework locally. A pure calibrated sweep evaluates 392 threshold, runner-up-margin, role-agreement, Judge, and hard-conflict configurations; reports per-label safety, coverage, abstention, latency, cost, and tokens; selects on validation and verifies on held-out acceptance; and emits a versioned disabled policy artifact with dataset/configuration hashes and the 90/12 rollback snapshot. Runtime automatic assignment now fails closed without a compatible calibrated probability, and `aiCompleted` is split into `requiredRolesCompleted` and `matcherValidatorAgreement`; the calibration feature contract advanced to v2. The actual 30-case Phase 3 artifact selected no policy because it is not ready and is feature-incompatible. Schedule passes 110/110, React builds, 13 syntax checks pass, and the 392-configuration blocked run completes. No migration, database write, deployment, remote setting, policy activation, automatic link, or Phase 5 work occurred.
- 2026-08-30 -- Completed Schedule activity-assignment Phase 3.1 locally. Added an explicit six-label review contract, an additive backend-only MAIN schema and service-role RPC, authenticated Hebrew review choices, server-owned forbidden candidate keys, conflict-safe canonical-link precedence, and readiness reporting against a 100-case target. The live read-only refresh preserved 28 confirmed links and 2 rejected cases, including 24 explicitly linked legacy rows, but found zero shared explicit labels. Another 70 reviewed cases are required, with `no_match`, `stale_activity`, `irrelevant_alert`, and `ambiguous` still missing. Schedule tests pass 110/110, the React production build passes, and syntax checks pass for the changed entry points. The later activation checkpoint applied the migration, but deployment and evidence collection remain incomplete.
- 2026-08-30 -- Completed Schedule activity-assignment calibration Phase 3 locally. The runtime now separates `rankingScore`/`rankingGap` from optional `calibratedProbability`, carries versioned artifact compatibility through evaluation and review UI, and labels raw values as `ציון התאמה` without a percent sign. The full 30-case hybrid-20 run had 100% final in-set recall, 50% top-1 accuracy, zero role failures, and zero writes. Confirmed and rejected cases both averaged 57.81 raw score. Platt won validation but was worse than raw control on held-out Brier, so the 30-case artifact is `readyForProduction: false` and emits no probability. Schedule tests pass 102/102 and React build passes. No threshold, margin, remote setting, database row, deployment, or automatic link changed.
- 2026-08-30 -- Completed local Phase 2 evaluation for Schedule activity-assignment retrieval. Added backward-compatible deterministic-first, full-semantic, and hybrid-union strategies; versioned cached activity embeddings; bounded semantic concurrency; partial-failure safety; construction vocabulary corrections; retrieval-only and targeted evaluation lanes. The frozen 30-case retrieval run improved recall at 5 from 82.14% to 89.29% with 132 embedding calls, while a targeted seven-case full-model run retained both rejected cases as conflicts with zero automatic writes. The recommended hybrid-20 configuration remains unactivated pending approval.
- 2026-08-30 -- Added and applied the MAIN shared Schedule assignment-review queue. New ambiguous agent results persist a safe top-two candidate snapshot, survive refresh and teammate sessions, hydrate into the Schedule table, and transition to selected/rejected after review while KAPAIM remains the link source of truth. The backend-only table/RPC privileges and RLS were catalog-verified, a transactional live insert/resolve test rolled back cleanly, the live repository read returned zero pending rows, Schedule passed 85/85, and React rebuilt successfully.
- 2026-08-23 -- Added accessible, sticky per-column filters to Schedule activity updates: kind, business-date range, content, severity, status, assignment state, and assigned activity name. Filters compose with the global search, update visible/assigned counts, reset together, and scope fresh/restarted sequential agent batches to the displayed set. Added pure filter regressions, passed 83/83 Schedule tests, rebuilt React, and verified the 1,065-row local table with no browser console errors.
- 2026-08-21 -- Integrated Schedule Activity Assignment with the shared Workflow inspector. Per-row and sequential runs now produce selectable graph/history entries with every pipeline component, inputs/outputs, model and scoring parameters, candidate signals, policy gates, audit/write outcomes, trace logs, and real OpenRouter token/cost/duration telemetry. Full prompts remain protected behind hashes. Failed runs retain an error graph. The proposed audit migration also persists `workflow_log`/`run_events` for serverless-safe history, with local fallback until it is applied. Schedule passes 75/75 and React builds.
- 2026-08-21 -- Fixed human-review assignment results so they ask the user to choose between the agent's top one or two activity candidates. Candidate display is independent of audit-schema availability; persisted runs use the review route and otherwise explicit user choice uses the existing manual assignment endpoint. Added accessible busy/focus states, cleared stale results after save, updated the spec, and passed 74/74 Schedule tests plus the React build.
- 2026-08-21 -- Added the batch-only optional Schedule time-relevance pre-filter. The UI exposes a default-off checkbox and skipped-row/status counts; the server applies deterministic signals before a configurable model role and skips only confident negatives, while all uncertainty/errors fail open. Manual row runs remain full-pipeline. Settings now includes the role, prompt, model parameters, and an 80% skip threshold. Schedule passes 73/73, React builds, and the live local page shows the control with 1,998 eligible alerts plus the new Settings controls.
- 2026-08-21 -- Added the controlled sequential “locate all” runner to Schedule activity updates. It reuses the per-row agent one at a time, prevents double/parallel starts, offers safe stop-after-current plus resume-from-position or fresh restart, exposes accessible progress and outcome counts, and locks conflicting row actions while active. The specification now records the approved scope amendment; Schedule passes 71/71 and the React bundle rebuilds.
- 2026-08-21 -- Implemented Schedule Activity Assignment Agent V1 end to end: pure scored candidate engine, prompt-injection-safe role parsing, bounded OpenRouter orchestration, embeddings, server policy/writer gates, reviewer confirm/reject/read APIs, Settings-owned credentials/configuration and dry-run lab, per-row Hebrew UI, audit/provenance migration, and six focused regressions. Schedule passes 66/66, React builds, browser verification loads KAPAIM's 102-task schedule and renders the agent action on alert rows with no console errors. The migration remains local/unapplied, so live automatic writes stay fail-closed.
- 2026-08-21 -- Grouped active Schedule alert cards into a single collapsible alerts panel and made both alerts and the relative contract-time ledger load collapsed by default. Both headers expose accessible expanded state and visible focus treatment. Schedule tests remain 60/60 and the React bundle rebuilt successfully.
- 2026-08-21 -- Centralized the Schedule chart's requested default controls: axes selected, all activities visible, red lateness spans hidden, and the as-of marker hidden/scale compacted. A regression locks the four-state default; Schedule tests pass 60/60 and the React bundle rebuilt successfully.
- 2026-08-20 -- Replaced the browser-locale-dependent contract-event date input with validated Israeli day/month/year entry and made blue trigger flags use the same display formatter. The regression proves `11/12/2024` becomes `2024-12-11`, rejects impossible dates, and round-trips correctly. Schedule tests pass 59/59 and the React bundle rebuilt successfully.
- 2026-08-20 -- Added an independent “hide as-of and compact axis” control. It removes the as-of line/label and dependent red spans, excludes the as-of date from scale bounds, and derives the compact range from all schedule activities plus contractual/update markers rather than the filtered visible subset. Schedule tests pass 58/58 and the React bundle rebuilt successfully.
- 2026-08-20 -- Added a persistent per-project completion-date control and a presentation-only red-lateness-span toggle. Historical projects now stop health, sweep, and alert-scan calculations at the saved completion date unless the user supplies an explicit “as of” override. The settings update is same-origin authenticated and preserves the project's existing JSON settings. Schedule tests pass 57/57 and the React bundle rebuilt successfully.
- 2026-08-20 -- Replaced the hardcoded MAIN `_test` Gantt reader with project-aware routing derived from each active project's database and validated `projects.settings` table names. Sweep, single-indicator, and activity-alert assignment validation now load Gantt data for the requested active project rather than the Contracts mapping's source project. The project selector lists both databases and shows project names. Live loader proof returned one KAPAIM file and all 102 tasks; Schedule tests passed 55/55 and the React bundle rebuilt successfully.
- 2026-08-20 -- Corrected the diagnosis after the user supplied the project-to-table mapping. The active KAPAIM project uses unsuffixed `gantt_files`/`gantt_tasks`, while the MAIN general project uses `_test`. The runtime currently hardcodes MAIN `_test`, and the prior clean-reset deletion therefore affected the MAIN general project rather than KAPAIM. Its old XML object remains in MAIN Storage for recovery. No further data write was performed after discovering the mismatch.
- 2026-08-20 -- Diagnosed the first post-reset production upload attempt. MAIN remained at zero Gantt files and zero tasks, Storage contained no new XML object, and recent Supabase evidence showed only Schedule reads with no upload/write request. The empty Schedule UI is a missing-ingestion result, not a chart-rendering failure.
- 2026-08-20 -- Per the user's explicit clean-reset request, deleted the single MAIN `gantt_files_test` row; its `ON DELETE CASCADE` removed all 382 `gantt_tasks_test` rows. Removed the one stale KAPAIM `schedule_activity_alert_links` Gantt association. Verification returned zero files, zero tasks, and zero Gantt alert links; the prior XML Storage object remains intact for recovery.

- 2026-08-20 -- Corrected the Schedule activity-update workspace to use the exact Timeline alert source instead of `schedule_observed_events`. KAPAIM migration `20260820170934` adds backend-only `schedule_activity_alert_links` with RLS, service-role CRUD, source uniqueness, Gantt-key validation, updated-at trigger, and activity/date index. The shared Timeline alert loader now pages 500 rows at a time to avoid statement timeout while preserving its 2,000-item contract. The restarted local server returns exactly 2,000 items and zero links; 54 Schedule tests, alert Timeline mapping, React build, and both Schedule Playwright regressions pass. No fake link row, commit, push, or deployment occurred.
- 2026-08-20 -- Removed the `INDICATOR_CONTRACT_CONDITIONS_V1_APPROVED` runtime feature flag. Project mapping reads and approved contract-condition synchronization now run consistently in every configured APP DATA environment; only a genuinely unconfigured APP DATA connection uses the source project identity fallback. Database service-role, review, mapping, and atomic RPC gates remain intact.
- 2026-08-20 -- Fixed contract-condition milestone rendering on the Schedule chart: newly resolved contractual dates now remain in-range and visible as labeled flags under late-only filtering; added pure scale/subject helpers plus unit and Playwright regressions.
- 2026-08-20 -- Added durable provisional Schedule flags for already-saved working-day triggers when holiday coverage is incomplete. The read side deterministically re-derives the provisional date, while the final milestone remains blocked until calendar coverage is valid.
- 2026-08-20 -- Moved contractual marker labels from the bottom to a two-level rail above the Schedule chart and added a separate start-of-count marker at each saved trigger date. Added legend treatment, vertical guide lines, scale inclusion, and Playwright coverage for both start and target markers.

- 2026-08-19 -- Applied the Indicator contract-condition schema to KAPAIM, then corrected the inherited R5 guard so Indicator-owned relative decisions validate against the active project mapping without weakening the legacy direct-projection checks. The approved dry-run returned exactly four eligible and zero blocked; the first commit inserted four `pending` rows and the idempotency replay returned four unchanged. Catalog checks confirm all five RPCs are executable only by `service_role`. The local Schedule ledger now renders all four rules with trigger descriptions and manual date inputs. Schedule 50/50 and Contracts 152/152 pass.
- 2026-08-19 -- Implemented the Indicator-to-Schedule relative-condition bridge locally on `codex/contracts-relative-schedule`: review-triggered and sweep-start reconciliation, service-role-only transactional RPCs, structured-first trigger resolution, atomic milestone/condition lifecycle, calendar-coverage blocking, multiple-relative-mention splitting, secure source links, pending-table UI, and management status/retry APIs. Contracts 152/152 and Schedule 49/49 pass; React and syntax/diff checks pass. No live migration or Schedule row was written; local PostgreSQL compilation remains unavailable without Docker, and the worktree lacks the live KAPAIM settings needed for a fresh read-only dry run.
- 2026-08-18 -- Corrected the R5 ownership boundary. Contracts no longer exposes a Schedule-shadow planner or promotion gate; it provides a GET-only, zero-write Indicator handoff derived from reviewed `private.contracts` decisions. KAPAIM migration `20260818080957` removes the legacy projection RPC and Contracts-owned one-target uniqueness while preserving optional one-way provenance guards. The retained workspace now reports 40 suitable, 24 unsuitable, and 73 review-required decisions out of 137; Schedule target tables and linked rows remain zero. Project/activity mapping, target choice, cardinality, date/calendar calculation, and Schedule writes belong to the future Indicator agent.
- 2026-08-12 -- הושלם Phase 3G עד גבול schema-only. המאמת הייעודי עבר 25/25, Contracts עבר 96/96, Schedule עבר 47/47, וחבילת מסד Phase 3, מרוץ PostgreSQL ו־rollback לא־הרסני עברו. KAPAIM מתעד את מיגרציות `20260812200241` ו־`20260812200652`; בדיקת קטלוג האבטחה עברה, ומיפויים/אירועי ביקורת נשארו `0`. PID `60996` עלה עם Phase 3F.1 פעיל ושער 3G סגור; preview החזיר 409 `previous_version_missing` ו־apply החזיר 503 `not_approved`. לא נוצרו שורות Schedule או התראות. הדרישה הבאה היא להעלות גרסת MAIN שנייה אמיתית ולהריץ שוב preview בלבד; apply חי דורש אישור נפרד.
- 2026-08-12 -- Activated and certified Phase 3F.1 reuse. Remote migration `20260812152042` and private PDF-only `contracts-private` with a 3,000,000-byte cap are active; process-local server PID `39492` reported `ready: true`. The initial exact-PDF run saved one workspace and revision-1 draft with 12 candidates and logged eight model-call events: seven completions plus one 90-second chunk-7 failure before a successful `gpt-4o-mini` retry. Reopen and a byte-identical second upload restored the Hebrew draft with zero additional model calls, as the UI explicitly reported. Credential rotation remains recommended, user-accepted unresolved risk; no mapping, Schedule row, arithmetic, or alert changed.
- 2026-08-12 -- Completed Phase 3F.1 locally. Added private saved-workspace/draft schema and service-role-only RPCs, exact pre-model reuse, private PDF Storage transport, same-origin routes, Hebrew list/reopen/autosave UI, and a clear distinction between “search not run” and a real zero-alternative result. Contracts 88/88, Schedule 47/47, isolated database, Phase 2/3F UI, build, syntax, and desktop/mobile checks pass. No remote migration, bucket, flag, live workspace, mapping, alert, deployment, commit, or push occurred.
- 2026-08-12 -- Completed Phase 3F locally. Added the gated atomic-review service/routes, service-role-only immutable history reader, correction continuity, strict request/credential rejection, and desktop/390px Hebrew review UI. Contracts 75/75, Schedule 47/47, full isolated Phase 3 DB, rollback, Phase 2 UI regression, Phase 3F UI, build, syntax, and diff checks pass. No live migration apply, review RPC, mapping, alert, deployment, commit, or push occurred. Stop before Phase 3G.
- 2026-08-11 -- Completed Phase 3E. Added server-owned, same-origin read-only activity-list and candidate endpoints, exact request/credential rejection, typed fail-closed errors, and a metadata-only live verifier. Contracts 69/69 pass; the live approved route resolves one conflict-free MAIN version with 382 activities and 0 KAPAIM mappings. No review RPC, mapping row, Schedule calculation, alert, UI, deployment, commit, or push was added. Stop before Phase 3F.
- 2026-08-11 -- Completed Phase 3D on KAPAIM. The primary activity-mapping migration and separately approved composite-FK-index follow-up are recorded remotely; all 22 Phase 3 constraints validate, the nine planned indexes and immutable trigger exist, both RPCs remain invoker-only with empty `search_path`, and the missing-FK advisor finding is closed. Current mappings, mapping-review events, milestones, extensions, and conditions remain at zero. Stop before Phase 3E API work.
- 2026-08-11 -- Completed the approved Phase 3C local database package without applying it to KAPAIM. The existing `schedule_activity_map` is hardened in the proposed migration; immutable review events and two backend-only invoker RPCs implement atomic manual review and strict same-UID cross-version continuation. The clean isolated suite passed with 10 mapping rows, 6 review events, and 8 confirmed winners; schema lint/advisors and the non-destructive rollback also passed. Stop before Phase 3D live apply.
- 2026-08-11 -- Closed Phase 2 for the reviewed Herzliya sample. The production planner/writer asserted 12 rejected decisions, `review_only`, no global blockers, and zero planned operational rows before the live atomic RPC. KAPAIM now contains one immutable batch, 12 rejected decisions, and one zero-promotion attempt; no milestone, extension, or condition row was created. The temporary activation process was stopped. The first real promotion/ingestion proof is deferred until a genuinely eligible reviewed fact exists and does not block starting Indicator work.
- 2026-08-10 -- Completed the local Phase 2 migration/apply package. Added four proposed private audit/mapping tables, one `SECURITY INVOKER` atomic promotion RPC with empty `search_path`, a double-opt-in APP DATA writer, immutable failure/review audit design, and seven focused tests. Contracts 45/45, representatives 6/6, Schedule 47/47, syntax, React build, and diff checks pass. The SQL is unapplied and not PostgreSQL-compiled because the Supabase CLI/local stack is unavailable.
- 2026-08-10 -- Recorded CTO approval of the four Phase 2 architecture recommendations. The next bounded checkpoint must specify and test the exact additive schema, RPC, RLS/grants, and rollback plan; no live DDL or write was authorized or performed.
- 2026-08-10 -- Completed the Phase 2 read-only schema-reuse audit and pure promotion planner. Existing milestone, extension, and condition payloads can be mapped safely only when five approvals are explicit; no database write is implemented. Operational persistence is paused on project namespace, immutable review/audit storage, atomic promotion, and permission decisions. The audit also surfaced eight unrelated backup tables with RLS disabled for separate security review.
- 2026-08-10 -- Accepted Contracts Agent Phase 1 after the final 172.1-second live gold evaluation passed all 15 hard gates and all quality thresholds. The output remains dry-run-only with 12 candidates, one unresolved conflict, five missing-information rows, three packet gaps, null computed completion, and zero approved projections. Contracts 32/32, representatives 6/6, Schedule 47/47, React build, and production audit are green; no Schedule implementation file, table, DDL, or row changed.
- 2026-08-10 -- Completed the authorized 88.7-second Contracts replay with 12 candidates, one unresolved conflict, five canonical missing-information rows, perfect unchanged quality metrics, and no provider fallback. Two exactness gates remained; the compiler now prunes redundant compound steps from simple roles and treats placeholder appendix content as missing. Contracts 32/32, representatives 6/6, Schedule 47/47, and React build pass. One final live acceptance replay remains; no Schedule implementation file, table, DDL, or row changed.
- 2026-08-10 -- Added bounded single-segment fallback for malformed multi-segment drafts and completed a 150.2-second real-contract run with perfect unchanged quality metrics. Four deterministic exactness differences were corrected locally; 32/32 Contracts tests and 6/6 representative hashes pass. One post-fix replay remains before Phase 1 exit; no Schedule implementation file, table, DDL, or row changed.
- 2026-08-08 -- Completed the Contracts Agent Phase 0 evidence package: live read-only eight-table audit, existing-table reuse matrix, 47/47 Schedule regression baseline and protected hashes, output schema, page-grounded sample contract annotation, and representative synthetic variants. No runtime, database, or UI behavior changed.
- 2026-08-05 -- Moved the Gantt activity-label column to the left of the chart while preserving the timeline's left-to-right direction; aligned the time overlay and responsive 240/170px column offsets with the new layout.
- 2026-08-05 -- Split Schedule storage by responsibility after live verification: uploaded Gantt metadata/tasks are read from MAIN `gantt_files_test`/`gantt_tasks_test`, while calendars, contract data, snapshots, alerts, and conditions remain on APP DATA/KAPAIM. Browser verification loaded 1 schedule file and 382 tasks successfully.
- 2026-08-05 -- Corrected Schedule routing to reuse the single existing APP DATA (`contentSource`) URL/key and removed the mistakenly introduced Schedule/KAPAIM credentials. Live KAPAIM OpenAPI inspection verified the `app_data` profile (`gantt_files`/`gantt_tasks`) and showed that both source tables are empty; all required `schedule_*` tables exist, with only `schedule_calendars` containing one row.
- 2026-08-05 -- Added the pending contractual-condition pool and Schedule UI box.
- 2026-08-05 -- Added evidence-gated condition resolution through the existing chat RAG engine, deterministic due-date calculation, controlled promotion to milestones, API/UI integration, and regression tests.
- 2026-08-05 -- Replaced the bulk resolver control with a per-row contractual-condition table action and exact server-side `conditionId` isolation.
- 2026-08-05 -- Diagnosed live per-row resolver failure as an OpenRouter 401 (`User not found`) affecting both chat completion and embeddings; added actionable inline authentication errors and a Settings link.
- 2026-08-05 -- Confirmed the replacement MAIN Settings OpenRouter key works for chat and embeddings; the remaining live blocker is missing `schedule_alerts`, `schedule_contract_milestones`, `schedule_contract_conditions`, and `schedule_calendars` in the configured App Supabase Data API. Made optional Schedule loads fail independently so the contractor schedule remains usable with explicit warnings.

## Gotchas

- Never let an LLM calculate schedule offsets; only validated trigger evidence crosses into deterministic date arithmetic.
- A `found` result without both a pinpoint excerpt and a traceable source is treated as ambiguous.
- Recurring conditions remain pending and use the trigger date in the generated milestone key so later occurrences can create distinct milestones.
- The resolver uses one service-role-only RPC to upsert the milestone and transition the condition in the same transaction. Reruns are safe because milestone identity is stable.
- A configured key is not necessarily a valid key. The resolver maps OpenRouter 401/`User not found` to `openrouter_auth`; the UI must expose this instead of a generic search failure.
- Do not diagnose this resolver with `getConfig().openRouterApiKey` alone because generic config supports env fallback. Use the settings-owned boundary.
- A healthy Gantt source does not imply the Schedule schema is provisioned. `gantt_*` and every `schedule_*` table must be exposed by the KAPAIM Data API reached through APP DATA.
- PostgREST OpenAPI does not prove live indexes, constraints, triggers, RLS, policies, grants, or owners. Phase 2 therefore used direct catalog verification in addition to OpenAPI before the reviewed live closure.
- 2026-08-10 -- Completed the Contracts Phase 2 local operational package. Supabase CLI 2.113.0 is pinned; the primary migration compiled locally and was later aligned to remote history version 20260810175150; 50 Contracts tests, 47 Schedule tests, isolated database atomicity/RLS/role tests, schema lint, advisors, React build, and desktop/mobile rendering pass. Authenticated review routes and UI are wired; activation, live promotion, and deployment remain separately gated.
- A gold contract annotation is evaluation evidence, not operational approval. Authority, project binding, human review, exact evidence, and an approved persistence target remain separate gates.
- 2026-08-10 -- Applied and remotely verified the Contracts Phase 2 schema/mapping gate on KAPAIM. Remote migrations `20260810175150`, `20260810181135`, and `20260810183407` added the private review/audit model, atomic invoker RPC, browser-role privilege hardening, and mapping-FK index. One approved MAIN-to-KAPAIM mapping exists. The later 2026-08-11 closure added only the rejection-only audit evidence; target Schedule rows and deployment remain unchanged.

## Related

- See [chat.md](chat.md) for the RAG pipeline reused by the resolver.
- See [timeline.md](timeline.md) for the separate project Timeline UI.
