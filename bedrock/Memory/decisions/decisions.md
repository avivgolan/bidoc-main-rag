---
note_type: decisions-index
project: bidoc agent
status: active
last_updated: 2026-08-12
tags:
  - bedrock
  - decision
---

# Decisions

## Purpose

Architectural and process decisions that affect this project.

## Current State

- The existing Schedule Engine and Calendar are the canonical calculation baseline for Contracts and Indicators. Necessary, regression-tested integration extensions are allowed; parallel or duplicate schedule arithmetic is prohibited.
- The eight existing KAPAIM `schedule_*` tables are canonical and must be reused; zero DDL is the default.
- Contracts Agent Phase 1 dry-run is accepted. Phase 2 schema, mapping, transport, review UI, and rejection-only live audit closure are complete. The first real target-row promotion remains deferred to an eligible reviewed fact; it must reuse the existing Schedule ingestion and engine.
- Contracts Phase 3B through Phase 3F are complete through the schema-only boundary. The server-owned manual review/history API and Hebrew UI rebuild alternatives from current MAIN/KAPAIM state and preserve immutable correction history. KAPAIM records the Phase 3F history-read migration as `20260812200241`; its live write gate remains closed, and no review write, Schedule arithmetic, alert integration, or deployment is authorized.
- Contracts Phase 3F.1 exact saved-workspace reuse is activated. KAPAIM records remote migration `20260812152042`; `contracts-private` is private, PDF-only, and capped at 3,000,000 bytes. One exact-PDF workspace and revision-1 draft with 12 candidates were saved, and both reopen and a byte-identical second upload restored the Hebrew draft with zero additional model calls. Credential replacement/revocation remains recommended unresolved risk that the user explicitly accepted; no mapping, Schedule arithmetic, alert, or deployment was added.
- Phase 3G ממומש ומאומת עד גבול schema-only: התאמת גרסת העלאה חדשה סומכת רק על שתי גרסאות MAIN מתוארכות וסמכותיות, ספירות `Content-Range` מדויקות, מצב KAPAIM נוכחי והיסטוריה בלתי־ניתנת לשינוי. KAPAIM מתעד את ה־forward guard כ־`20260812200652`; קטלוג האבטחה נקי ושורות המיפוי/היסטוריה נשארו `0`. השרת PID `60996` פועל עם Phase 3F.1 פעיל ושער 3G סגור. preview חי נכשל סגור ב־`previous_version_missing`, וה־apply החי נדחה ב־`not_approved`. הדרישה הבאה היא גרסת MAIN שנייה אמיתית ואז preview נוסף; apply חי עדיין דורש אישור נפרד.

## Recent Changes

- 2026-08-12 - הושלם Phase 3G עד גבול schema-only. המאמת הייעודי עבר 25/25, Contracts עבר 96/96, Schedule עבר 47/47, וחבילת המסד, מרוץ PostgreSQL וה־rollback הלא־הרסני עברו. KAPAIM מתעד `20260812200241` להיסטוריית Phase 3F ו־`20260812200652` ל־guard של Phase 3G; קטלוג האבטחה נקי ומיפויים/אירועי ביקורת נשארו `0`. PID `60996` עלה עם Phase 3F.1 פעיל ושער 3G סגור; preview החזיר 409 `previous_version_missing` ו־apply החזיר 503 `not_approved`. לא בוצעו כתיבת מיפוי/היסטוריה, חישוב Schedule או התראה.
- 2026-08-12 - Activated and live-verified Phase 3F.1. KAPAIM migration `20260812152042`, private PDF-only `contracts-private` with a 3,000,000-byte cap, and process-local server PID `39492` reached `ready: true`. The first exact PDF saved one workspace and revision-1 draft with 12 candidates; eight model-call events comprised seven completions and one 90-second chunk-7 failure followed by a successful `gpt-4o-mini` retry. Reopen and a byte-identical upload restored the Hebrew draft with zero additional model calls. Credential rotation remains recommended and unresolved by explicit user risk acceptance.
- 2026-08-12 - Completed Phase 3F.1 locally. Added exact pre-model PDF reuse, immutable extraction and per-reviewer draft persistence, private-bucket enforcement, same-origin APIs, Hebrew reopen/autosave UX, and the corrected “search not run” mapping state. All focused Contracts, Schedule, database, UI, build, syntax, and responsive checks pass; remote apply, Storage provisioning, flag activation, live workspace creation, mapping writes, alerts, deployment, commit, and push remain unauthorized.
- 2026-08-12 - Completed Phase 3F locally. Added strict same-origin confirm/reject/correct/unmapped and history routes, server-owned reviewer/time/evidence preparation, a service-role-only history-read RPC migration, and desktop/mobile Hebrew review UI. Contracts 75/75 and isolated database/UI gates pass. The history migration remains unapplied and the review-write flag remains closed; no live RPC or mapping was created.
- 2026-08-11 - Completed Phase 3E server-owned read-only APIs. The activity list and candidate routes require a same-origin superadmin session, reject database/configuration and client activity-state overrides, resolve the private project mapping, and read MAIN/KAPAIM only. Live verification found 382 activities and 0 mappings; no review RPC or write was invoked.
- 2026-08-11 - Completed Phase 3D remote apply and verification on KAPAIM. Migrations `20260811170622` and `20260811171813` are recorded; the second is the separately approved composite-FK-index advisor fix. No mapping, mapping-review event, or Schedule target row was created.
- 2026-08-11 - Completed the exact Phase 3C migration/DCL, immutable mapping-review history, resolver/review RPCs, isolated SQL security/behavior suite, and non-destructive rollback. All local database gates pass; no KAPAIM apply or live mapping was performed.
- 2026-08-11 - Split rejection-only audit persistence from operational Schedule promotion. A dedicated backend route may store only a complete all-rejected review and must produce zero Schedule rows; the promotion route now refuses such batches. Server activation and any live write remain separate explicit gates.
- 2026-08-10 - Completed the local Phase 2 migration/apply package without changing Supabase. Audit/mapping storage is proposed in a non-exposed private schema; the public function is `SECURITY INVOKER`, uses an empty `search_path`, and is server-role-only; the writer requires commit plus migration-apply approval. Database compilation and apply remain separate gates.
- 2026-08-10 - Recorded CTO approval of D2-01 through D2-04. The approved direction is an additive MAIN-to-KAPAIM mapping, append-oriented audit storage, one atomic PostgreSQL promotion RPC, and backend-only execution with no anon/authenticated direct writes. This is architecture approval, not permission to apply DDL.
- 2026-08-10 - Recorded the Phase 2 schema-reuse gate: existing Schedule tables are reused, but no writer is authorized because cross-database project identity, immutable review state, atomicity, and permissions are unresolved. The planner must remain pure and fail closed until all five gate approvals are explicit.
- 2026-08-10 - Accepted Contracts Agent Phase 1 after the final live gold evaluation passed all unchanged hard gates and quality thresholds within the global budget; recorded the redacted accepted artifact and preserved the separate Phase 2 approval boundary.
- 2026-08-08 - Recorded the CTO's protected Schedule baseline, existing-table reuse lock, and phase-gated Contracts Agent boundary.
- 2026-08-10 - Recorded the CTO clarification that Schedule Engine files are not immutable. Contracts/Indicator work may build on and modify the existing Engine when required, provided existing logic is reused, unchanged behavior is regression-protected, and no parallel calculation engine is created.
- 2026-05-08 - Created decision log.

## Decisions

- 2026-08-08 - Preserve `src/scheduleEngine.js`, `src/scheduleCalendar.js`, and existing Schedule semantics. Contracts work integrates additively; any behavioral change requires a separate bounded approval.
- 2026-08-08 - Reuse `schedule_calendars`, `schedule_contract_milestones`, `schedule_contract_extensions`, `schedule_contract_conditions`, `schedule_indicator_snapshots`, `schedule_alerts`, `schedule_activity_map`, and `schedule_observed_events`. Do not create duplicate-purpose tables or execute unapproved DDL.
- 2026-08-08 - Phase 1 may extract evidence-backed candidates only after the Phase 0 checkpoint is approved. It performs no operational database writes, Schedule arithmetic, or conflict resolution.
- 2026-08-10 - Phase 1 is accepted as local authenticated dry-run only. Thirty-two focused tests, six exact canonical representative cases, 47 Schedule tests, the React build, dependency audit, and the final real-contract gold evaluation pass. Compiler safety gates and thresholds remain unchanged. Phase 2 persistence and promotion require a separate approved slice.
- 2026-08-10 - Phase 2 may prepare table-compatible payloads only through the pure planner. No operational write may occur unless schema reuse, project namespace, immutable review/audit persistence, atomic promotion, and the permission model are all explicitly approved.
- 2026-08-10 - The CTO approved D2-01 through D2-04 as recommended: a bounded additive project mapping; separate immutable audit persistence; one transactional PostgreSQL promotion RPC; and backend-only least-privilege execution. The exact SQL, tests, permission diff, rollback plan, and live application still require a separate apply approval.
- 2026-08-10 - The local exact package is ready for the apply gate: four additive private tables, one atomic invoker RPC, a fail-closed backend transport, and rollback/test documentation. It must not be applied to KAPAIM until it compiles and passes isolated database/RLS/advisor tests.
- 2026-08-10 - The isolated Phase 2 database gate now passes and the authenticated reviewer workflow is implemented. Promotion remains fail closed until the official migration and reviewed MAIN-to-KAPAIM mapping are explicitly applied and verified remotely, after which only the server-side apply flag may enable commit.
- 2026-08-10 - The approved KAPAIM schema/mapping apply is complete. Remote verification found and removed legacy non-read browser privileges and added the missing mapping-FK index. Exactly one approved mapping is active; no contract fact was promoted. Activation, a reviewed live promotion, and deployment remain separate explicit gates.
- 2026-08-11 - A complete rejection-only Contracts batch is audit evidence, not a blocked promotion. It must use `/api/contracts/review/save`, remain all-rejected, and persist zero Schedule target rows; `/api/contracts/review/commit` is reserved for transaction-ready approved facts.
- 2026-08-11 - Phase 2 is closed for the reviewed Herzliya sample with one live `review_only` batch, 12 rejected decisions, one `reviewed_no_promotion` attempt, and zero Schedule target rows. Do not manufacture an approval to force an end-to-end write; the first real promotion and ingestion proof waits for a genuinely eligible fact and is a separate checkpoint.
- 2026-08-11 - Phase 3C is approved and complete as an isolated local package only. The migration may harden `schedule_activity_map`, add one immutable private review-event table, and expose service-role-only invoker RPCs with an empty `search_path`; the rollback must preserve mappings and audit evidence. Applying it to KAPAIM is Phase 3D and requires separate explicit CTO/security approval.
- 2026-08-11 - Phase 3D is complete. KAPAIM may retain the hardened mapping table, immutable private review-event table, service-role-only invoker RPCs, and the full composite project-mapping FK index. No initial mapping was authorized or created. Phase 3E server-owned read-only candidate APIs require separate approval.
- 2026-08-11 - Phase 3E is complete. Server-owned same-origin activity-list and candidate APIs may resolve the approved private mapping and read MAIN Gantt plus KAPAIM current mapping state. They must reject client database credentials and client-owned activity/mapping state, must never call the review RPC, and must report no writes. Phase 3F manual actions and UI require separate approval.
- 2026-08-12 - Phase 3F is complete through schema-only apply. Manual review may expose only confirm, reject, correct, and unmapped through the server-owned atomic RPC; every submission must rebuild current alternatives and bind the authenticated reviewer, server time, exact evidence, and immutable supersession chain. The history migration is present as `20260812200241`, but live review writes remain prohibited until the exact server activation flag receives separate approval. Phase 3G remains a separate gate.
- 2026-08-12 - The user explicitly accepted activating Phase 3F.1 without first rotating the KAPAIM backend credential exposed to internal task output. This unblocks the saved-workspace functional checkpoint only; replacement and revocation remain recommended unresolved security work, and the credential value must never be copied into documentation or logs.
- 2026-08-12 - Phase 3G רשאי להמשיך מיפוי בין העלאות רק כאשר UID, שם מנורמל, רמת היררכיה, ניתוב פרויקט, בחירת גרסה, ייחודיות וביטחון `>=0.95` מאומתים מול המקורות הסמכותיים. ה־guard קיים ב־KAPAIM כ־`20260812200652`, אך שער ההחלה נשאר סגור. הדרישה הבאה היא העלאת גרסת MAIN שנייה אמיתית והרצת preview חוזרת; אין להריץ apply חי ללא אישור נפרד ומפורש. המימוש אינו מקבל `auto_continue` מהדפדפן, אינו משנה אישור ידני קיים, ואינו מפעיל Schedule Engine או כותב התראה.

## Open Questions

- Which future reviewed contract fact will first satisfy trigger, calendar, project-binding, conflict, and target-table requirements for the separate real-promotion checkpoint?
